// M4 deliverable: gmap API server (WebSocket + REST).
// Populated in Milestone 4.

export const DEFAULT_PORT = 7842;
export const DEFAULT_HOST = '127.0.0.1';

export async function startServer(_port = DEFAULT_PORT): Promise<void> {
  // stub
}
