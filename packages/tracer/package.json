{
  "name":    "@gmap/tracer",
  "version": "0.1.0",
  "private": true,
  "type":    "module",
  "main":    "./dist/index.js",
  "scripts": {
    "build":     "tsc --build",
    "typecheck": "tsc --build",
    "test":      "vitest run",
    "lint":      "eslint src",
    "clean":     "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@gmap/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "20.16.5"
  }
}
