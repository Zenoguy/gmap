{
  "name":    "@gmap/cli",
  "version": "0.1.0",
  "private": false,
  "type":    "module",
  "main":    "./dist/index.js",
  "bin": {
    "gmap": "./dist/index.js"
  },
  "scripts": {
    "build":     "tsc --build",
    "typecheck": "tsc --build",
    "dev":       "tsc --build --watch",
    "test":      "vitest run",
    "lint":      "eslint src",
    "clean":     "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@gmap/core": "workspace:*",
    "commander":  "12.1.0",
    "chalk":      "5.3.0"
  },
  "devDependencies": {
    "@types/node": "20.16.5"
  }
}
